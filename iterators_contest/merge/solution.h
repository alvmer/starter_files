#pragma once

#include <vector>

using iterator = std::vector<int>::iterator;

iterator Merge(iterator first_one, iterator last_one, 
               iterator first_two, iterator last_two, 
               iterator first_dest) {
    // your code here
}
